name: Strategy 70 ML selector proof
on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/search_strategy_70_ml_selector.py'
      - '.github/workflows/strategy-70-ml-selector.yml'
permissions:
  contents: read
jobs:
  ml-selector-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r requirements.txt scikit-learn
      - name: Run leakage-controlled ML selector proof
        run: python scripts/search_strategy_70_ml_selector.py
      - uses: actions/upload-artifact@v4
        with:
          name: strategy-70-ml-selector
          path: strategy_70_ml_selector.json
          if-no-files-found: error
