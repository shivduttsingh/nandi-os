name: Strategy 70 M-W proof

on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/search_strategy_70_mw.py'
      - '.github/workflows/strategy-70-mw.yml'

permissions:
  contents: read

jobs:
  mw-70-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r requirements.txt
      - name: Run M-W monthly walk-forward
        run: python scripts/search_strategy_70_mw.py
      - uses: actions/upload-artifact@v4
        with:
          name: strategy-70-mw
          path: strategy_70_mw.json
          if-no-files-found: error
